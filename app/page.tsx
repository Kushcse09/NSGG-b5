'use client'

import { useMemo, useState } from 'react'
import { Activity, AlertCircle, ArrowDownRight, ArrowUpRight, Bell, CalendarDays, Check, ChevronDown, ChevronLeft, ChevronRight, Clock3, Command, FileText, LayoutDashboard, Menu, MoreHorizontal, Phone, Plus, RefreshCw, Search, Settings, ShieldCheck, UserRound, Users, X, Zap } from 'lucide-react'

type Status = 'Confirmed' | 'Needs attention' | 'Rescheduled' | 'Cancelled' | 'No answer'
type Appointment = { id: number; initials: string; name: string; phone: string; service: string; time: string; date: string; status: Status; staff: string; staffInitials: string; attempts: number; last: string; prep: string }

const appointments: Appointment[] = [
  { id: 1, initials: 'JM', name: 'Jordan Miller', phone: '(415) 555-0198', service: 'Initial consultation', time: '9:00 AM', date: 'Today', status: 'Confirmed', staff: 'Alex Kim', staffInitials: 'AK', attempts: 1, last: 'Today, 8:14 AM', prep: 'Jordan mentioned they are bringing prior lab results and would like to discuss a recurring shoulder issue.' },
  { id: 2, initials: 'AR', name: 'Avery Rodriguez', phone: '(415) 555-0144', service: 'Follow-up visit', time: '10:30 AM', date: 'Today', status: 'No answer', staff: 'Maya Chen', staffInitials: 'MC', attempts: 3, last: 'Today, 8:42 AM', prep: 'No confirmation received after three attempts. The client previously asked if a later slot is available.' },
  { id: 3, initials: 'SP', name: 'Sofia Patel', phone: '(415) 555-0112', service: 'Annual check-up', time: '11:15 AM', date: 'Today', status: 'Confirmed', staff: 'Alex Kim', staffInitials: 'AK', attempts: 1, last: 'Yesterday, 11:02 AM', prep: 'Confirmed by phone. Sofia asked for a reminder to bring her current medication list.' },
  { id: 4, initials: 'DB', name: 'Daniel Brooks', phone: '(415) 555-0171', service: 'New patient intake', time: '1:00 PM', date: 'Today', status: 'Rescheduled', staff: 'Priya Shah', staffInitials: 'PS', attempts: 2, last: 'Today, 7:58 AM', prep: 'Requested a new time due to a work conflict. Moved from 11:00 AM to 1:00 PM.' },
  { id: 5, initials: 'LC', name: 'Lena Chen', phone: '(415) 555-0163', service: 'Medication review', time: '2:30 PM', date: 'Today', status: 'Confirmed', staff: 'Maya Chen', staffInitials: 'MC', attempts: 1, last: 'Today, 8:25 AM', prep: 'Confirmed attendance and asked whether a family member can join the call.' },
  { id: 6, initials: 'KW', name: 'Kai Williams', phone: '(415) 555-0136', service: 'Follow-up visit', time: '8:30 AM', date: 'Tomorrow', status: 'Confirmed', staff: 'Priya Shah', staffInitials: 'PS', attempts: 1, last: 'Today, 8:31 AM', prep: 'Confirmed. No additional requests captured.' },
]

const activity = [
  ['10:42 AM', 'Call completed', 'Avery Rodriguez', 'No confirmation received', 'warning'],
  ['10:39 AM', 'Appointment confirmed', 'Lena Chen', 'Confirmed via phone', 'success'],
  ['10:31 AM', 'Reminder sent', 'Daniel Brooks', 'SMS + voice reminder', 'info'],
  ['10:18 AM', 'Call completed', 'Jordan Miller', 'Confirmed attendance', 'success'],
]

const statusStyles: Record<Status, string> = { Confirmed: 'bg-emerald-50 text-emerald-700 ring-emerald-200', 'Needs attention': 'bg-amber-50 text-amber-700 ring-amber-200', Rescheduled: 'bg-blue-50 text-blue-700 ring-blue-200', Cancelled: 'bg-red-50 text-red-700 ring-red-200', 'No answer': 'bg-amber-50 text-amber-700 ring-amber-200' }
function StatusPill({ status }: { status: Status }) { return <span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-[11px] font-medium ring-1 ${statusStyles[status]}`}><span className="size-1.5 rounded-full bg-current" />{status}</span> }
function Avatar({ initials, tone = 'indigo' }: { initials: string; tone?: string }) { return <span className={`grid size-8 shrink-0 place-items-center rounded-full text-[10px] font-semibold ${tone === 'teal' ? 'bg-teal-100 text-teal-700' : tone === 'amber' ? 'bg-amber-100 text-amber-700' : 'bg-indigo-100 text-indigo-700'}`}>{initials}</span> }
function Sparkline({ down = false }: { down?: boolean }) { return <svg viewBox="0 0 80 24" className={down ? 'h-6 w-20 text-amber-500' : 'h-6 w-20 text-emerald-500'} fill="none" aria-hidden="true"><path d={down ? 'M1 5c10 4 15 2 23 9s14-2 22 4 16-4 33 2' : 'M1 21c10-2 13 1 22-6s14 2 22-4 16 1 34-9'} stroke="currentColor" strokeWidth="2" strokeLinecap="round" /></svg> }

export default function App() {
  const [active, setActive] = useState('Dashboard'); const [collapsed, setCollapsed] = useState(false); const [selected, setSelected] = useState<Appointment | null>(null); const [query, setQuery] = useState(''); const [range, setRange] = useState('Today'); const [showAdd, setShowAdd] = useState(false); const [showCommand, setShowCommand] = useState(false); const [toast, setToast] = useState(''); const [detailTab, setDetailTab] = useState('Conversation')
  const filtered = useMemo(() => appointments.filter(a => [a.name, a.service, a.status, a.staff].some(v => v.toLowerCase().includes(query.toLowerCase())) && (range === 'All' || a.date === range)), [query, range])
  const notify = (message: string) => { setToast(message); window.setTimeout(() => setToast(''), 2600) }
  const nav = [['Dashboard', LayoutDashboard], ['Appointments', CalendarDays], ['Staff & Capacity', Users], ['Call Log', Phone], ['Settings', Settings]] as const
  return <div className="min-h-screen bg-slate-50 text-slate-900">
    <aside className={`fixed inset-y-0 left-0 z-30 hidden flex-col border-r border-slate-200 bg-white transition-all lg:flex ${collapsed ? 'w-[76px]' : 'w-[236px]'}`}>
      <div className="flex h-16 items-center px-5">{collapsed ? <div className="grid size-8 place-items-center rounded-lg bg-indigo-600 text-white"><ShieldCheck size={17} /></div> : <div className="flex items-center gap-2.5"><div className="grid size-8 place-items-center rounded-lg bg-indigo-600 text-white"><ShieldCheck size={17} /></div><span className="text-[15px] font-semibold tracking-tight">NoShow<span className="text-indigo-600">Guard</span></span></div>}</div>
      <div className="px-3"><button onClick={() => setShowAdd(true)} className="flex w-full items-center justify-center gap-2 rounded-lg bg-indigo-600 px-3 py-2.5 text-[12px] font-semibold text-white shadow-sm transition hover:bg-indigo-700"><Plus size={15} />{!collapsed && 'Add appointment'}</button></div>
      {!collapsed && <button onClick={() => notify('Location switched to Eastside Office')} className="mx-3 mt-5 flex items-center gap-2 rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-left"><span className="size-2 rounded-full bg-emerald-500" /><span className="min-w-0 flex-1"><span className="block text-[10px] text-slate-500">Location</span><span className="block truncate text-[11px] font-medium">North Clinic</span></span><ChevronDown size={13} className="text-slate-400" /></button>}
      <nav className="mt-7 flex-1 px-3"><p className={`mb-2 px-3 text-[10px] font-semibold uppercase tracking-[0.15em] text-slate-400 ${collapsed ? 'text-center' : ''}`}>{collapsed ? '•' : 'Workspace'}</p>{nav.map(([label, Icon]) => <button key={label} onClick={() => setActive(label)} title={label} className={`mb-1 flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-[12px] font-medium transition ${active === label ? 'bg-indigo-50 text-indigo-700' : 'text-slate-500 hover:bg-slate-50 hover:text-slate-900'} ${collapsed ? 'justify-center' : ''}`}><Icon size={16} />{!collapsed && label}</button>)}</nav>
      {!collapsed && <div className="m-3 rounded-lg border border-slate-200 bg-slate-50 p-3"><div className="flex items-center gap-2 text-[11px] font-medium text-slate-700"><span className="size-2 rounded-full bg-emerald-500" />Calendar synced</div><p className="mt-1.5 text-[10px] text-slate-500">Google Calendar · North Clinic</p><button onClick={() => notify('Calendar sync is up to date')} className="mt-2 text-[10px] font-medium text-indigo-600">Manage connection <ChevronRight size={12} className="inline" /></button></div>}
      <button onClick={() => setCollapsed(!collapsed)} className="m-3 flex items-center justify-center rounded-lg border border-slate-200 py-2 text-slate-400 hover:text-slate-700">{collapsed ? <ChevronRight size={16} /> : <ChevronLeft size={16} />}</button>
    </aside>
    <div className={`${collapsed ? 'lg:pl-[76px]' : 'lg:pl-[236px]'}`}>
      <header className="sticky top-0 z-20 flex h-16 items-center justify-between border-b border-slate-200 bg-white/95 px-4 backdrop-blur sm:px-7"><div className="flex items-center gap-3"><button className="text-slate-500 lg:hidden"><Menu size={20} /></button><div className="lg:hidden flex items-center gap-2 text-sm font-semibold"><span className="grid size-7 place-items-center rounded-md bg-indigo-600 text-white"><ShieldCheck size={15} /></span>NoShowGuard</div><div className="hidden items-center gap-2 text-[12px] text-slate-400 sm:flex"><span>Workspace</span><ChevronRight size={13} /><span className="font-medium text-slate-700">{active}</span></div></div><div className="flex items-center gap-2"><button onClick={() => setShowCommand(true)} className="hidden h-9 items-center gap-2 rounded-lg border border-slate-200 bg-slate-50 px-3 text-[11px] text-slate-400 hover:text-slate-700 sm:flex"><Search size={14} /> Search anything <kbd className="ml-4 rounded border border-slate-200 bg-white px-1.5 py-0.5 text-[9px]">⌘ K</kbd></button><button onClick={() => notify('No new notifications')} className="grid size-9 place-items-center rounded-lg text-slate-400 hover:bg-slate-100 hover:text-slate-700"><Bell size={17} /></button><div className="mx-1 h-5 w-px bg-slate-200" /><button className="flex items-center gap-2 rounded-lg p-1.5 hover:bg-slate-100"><Avatar initials="AK" /><ChevronDown size={13} className="text-slate-400" /></button></div></header>
      <main className="mx-auto max-w-[1480px] p-4 sm:p-7">
        {active === 'Dashboard' && <><div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 flex items-center gap-2 text-[11px] font-medium text-indigo-600"><span className="size-1.5 rounded-full bg-indigo-600" />Tuesday, September 13, 2026</p><h1 className="text-2xl font-semibold tracking-tight text-slate-900 sm:text-[28px]">Good morning, Alex</h1><p className="mt-1 text-[13px] text-slate-500">Here&apos;s what&apos;s happening with your appointments.</p></div><div className="flex items-center gap-2"><button onClick={() => setRange(range === 'Today' ? 'Tomorrow' : 'Today')} className="flex h-9 items-center gap-2 rounded-lg border border-slate-200 bg-white px-3 text-[11px] font-medium text-slate-700"><CalendarDays size={14} className="text-slate-400" />{range}<ChevronDown size={13} className="text-slate-400" /></button><button onClick={() => notify('Dashboard data refreshed')} className="grid size-9 place-items-center rounded-lg border border-slate-200 bg-white text-slate-400 hover:text-slate-700"><RefreshCw size={14} /></button></div></div>
          <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[['Today\'s appointments','24','12.5%','vs. 21 last Tuesday',CalendarDays,false],['Confirmed','21','4.2%','87.5% confirmation rate',Check,false],['Needs attention','3','20.0%','1 fewer than yesterday',AlertCircle,true],['Rescheduled this week','8','28.6%','2 more than last week',Clock3,false]].map(([label,value,change,sub,Icon,down]) => <div key={String(label)} className="rounded-lg border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-start justify-between"><div className="grid size-8 place-items-center rounded-lg bg-indigo-50 text-indigo-600"><Icon size={16} /></div><Sparkline down={Boolean(down)} /></div><div className="mt-4 flex items-end justify-between"><div><p className="text-[12px] text-slate-500">{String(label)}</p><p className="mt-1 text-2xl font-semibold tracking-tight text-slate-900">{String(value)}</p></div><span className={`mb-1 flex items-center text-[11px] font-medium ${down ? 'text-amber-600' : 'text-emerald-600'}`}>{down ? <ArrowDownRight size={13} /> : <ArrowUpRight size={13} />}{String(change)}</span></div><p className="mt-2 text-[11px] text-slate-400">{String(sub)}</p></div>)}</div>
          <div className="mt-6 grid gap-5 xl:grid-cols-[1fr_320px]"><section className="min-w-0 rounded-lg border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-3 border-b border-slate-200 p-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-sm font-semibold text-slate-900">Appointments</h2><p className="mt-1 text-[11px] text-slate-500">Default view · Today + Tomorrow</p></div><div className="flex items-center gap-2"><div className="flex h-8 items-center gap-2 rounded-lg border border-slate-200 px-2.5"><Search size={13} className="text-slate-400" /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Filter appointments" className="w-32 bg-transparent text-[11px] outline-none placeholder:text-slate-400" /></div><button onClick={() => notify('Filters are ready')} className="grid size-8 place-items-center rounded-lg border border-slate-200 text-slate-400"><SlidersHorizontalIcon /></button></div></div><div className="flex items-center gap-1 border-b border-slate-200 px-4 pt-3"><button onClick={() => setRange('Today')} className={`border-b-2 px-3 pb-3 text-[11px] font-medium ${range === 'Today' ? 'border-indigo-600 text-indigo-700' : 'border-transparent text-slate-400'}`}>Today <span className="ml-1 rounded bg-slate-100 px-1.5 py-0.5 text-[9px]">5</span></button><button onClick={() => setRange('Tomorrow')} className={`border-b-2 px-3 pb-3 text-[11px] font-medium ${range === 'Tomorrow' ? 'border-indigo-600 text-indigo-700' : 'border-transparent text-slate-400'}`}>Tomorrow <span className="ml-1 rounded bg-slate-100 px-1.5 py-0.5 text-[9px]">1</span></button><button onClick={() => setRange('All')} className={`border-b-2 px-3 pb-3 text-[11px] font-medium ${range === 'All' ? 'border-indigo-600 text-indigo-700' : 'border-transparent text-slate-400'}`}>All</button></div><div className="hidden overflow-x-auto md:block"><table className="w-full text-left"><thead className="bg-slate-50 text-[10px] uppercase tracking-wider text-slate-400"><tr>{['Client','Assigned staff','Service','Scheduled time','Status','Attempts','Last contacted',''].map(h => <th key={h} className="whitespace-nowrap px-4 py-3 font-medium">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{filtered.map(a => <tr key={a.id} onClick={() => setSelected(a)} className="cursor-pointer transition hover:bg-slate-50"><td className="px-4 py-3"><div className="flex items-center gap-2.5"><Avatar initials={a.initials} /><div><p className="text-[12px] font-medium text-slate-800">{a.name}</p><p className="mt-0.5 text-[10px] text-slate-400">{a.phone}</p></div></div></td><td className="px-4 py-3"><div className="flex items-center gap-2"><Avatar initials={a.staffInitials} tone="teal" /><span className="text-[11px] text-slate-600">{a.staff}</span></div></td><td className="px-4 py-3 text-[11px] text-slate-600">{a.service}</td><td className="whitespace-nowrap px-4 py-3 text-[11px] font-medium text-slate-700">{a.date}, {a.time}</td><td className="px-4 py-3"><StatusPill status={a.status} /></td><td className="px-4 py-3 text-[11px] text-slate-500">{a.attempts} of 3</td><td className="whitespace-nowrap px-4 py-3 text-[11px] text-slate-400">{a.last}</td><td className="px-4 py-3"><MoreHorizontal size={15} className="text-slate-400" /></td></tr>)}</tbody></table>{filtered.length === 0 && <div className="p-12 text-center"><CalendarDays className="mx-auto text-slate-300" size={26} /><p className="mt-3 text-sm font-medium text-slate-700">No appointments yet</p><p className="mt-1 text-xs text-slate-400">Connect your calendar or add one manually.</p><div className="mt-4 flex justify-center gap-2"><button onClick={() => notify('Calendar connection started')} className="rounded-lg bg-indigo-600 px-3 py-2 text-[11px] font-semibold text-white">Connect Calendar</button><button onClick={() => setShowAdd(true)} className="rounded-lg border border-slate-200 px-3 py-2 text-[11px] font-medium text-slate-600">Add manually</button></div></div>}</div><div className="grid gap-3 p-3 md:hidden">{filtered.map(a => <button key={a.id} onClick={() => setSelected(a)} className="rounded-lg border border-slate-200 p-3 text-left"><div className="flex items-center justify-between"><div className="flex items-center gap-2"><Avatar initials={a.initials} /><span className="text-xs font-medium">{a.name}</span></div><StatusPill status={a.status} /></div><div className="mt-3 flex justify-between text-[11px] text-slate-500"><span>{a.date}, {a.time}</span><span>{a.staff}</span></div></button>)}</div></section>
            <aside className="rounded-lg border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-slate-200 p-4"><div><h2 className="text-sm font-semibold text-slate-900">Live activity</h2><p className="mt-1 text-[11px] text-slate-500">Today, September 13</p></div><Activity size={16} className="text-indigo-600" /></div><div className="p-4">{activity.map(([time,title,name,detail,kind],i) => <div key={title+name} className="relative flex gap-3 pb-5 last:pb-0"><div className={`relative z-10 mt-0.5 grid size-6 shrink-0 place-items-center rounded-full border border-slate-200 bg-white ${i === 0 ? 'ring-4 ring-indigo-50' : ''}`}>{kind === 'warning' ? <AlertCircle size={12} className="text-amber-500" /> : kind === 'success' ? <Check size={12} className="text-emerald-500" /> : <Phone size={12} className="text-indigo-600" />}</div>{i < activity.length - 1 && <div className="absolute left-3 top-6 h-full w-px bg-slate-200" />}<div className="min-w-0"><div className="flex items-baseline justify-between gap-2"><p className="truncate text-[11px] font-medium text-slate-700">{title}</p><time className="shrink-0 text-[10px] text-slate-400">{time}</time></div><p className="mt-1 text-[11px] text-slate-500">{name}</p><p className="mt-1 text-[10px] text-slate-400">{detail}</p></div></div>)}</div><button onClick={() => setActive('Call Log')} className="flex w-full items-center justify-center gap-1 border-t border-slate-200 py-3 text-[11px] font-medium text-indigo-600">View call log <ChevronRight size={13} /></button></aside></div></>}
        {active === 'Appointments' && <Page title="Appointments" subtitle="Manage every appointment and confirmation."><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{appointments.map(a => <button key={a.id} onClick={() => setSelected(a)} className="rounded-lg border border-slate-200 bg-white p-4 text-left shadow-sm hover:border-indigo-200"><div className="flex items-center justify-between"><span className="text-xs font-medium">{a.name}</span><StatusPill status={a.status} /></div><p className="mt-2 text-[11px] text-slate-500">{a.date} · {a.time} · {a.staff}</p><p className="mt-1 text-[11px] text-slate-400">{a.service}</p></button>)}</div></Page>}
        {active === 'Staff & Capacity' && <Page title="Staff & Capacity" subtitle="See appointment load and confirmation coverage by consultant."><div className="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm"><table className="w-full text-left"><thead className="bg-slate-50 text-[10px] uppercase tracking-wider text-slate-400"><tr>{['Staff member','Appointments today','Confirmation rate','Booked capacity',''].map(h => <th key={h} className="px-5 py-3 font-medium">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{[['Alex Kim','8','92%','8 / 10','AK'],['Maya Chen','7','84%','7 / 8','MC'],['Priya Shah','5','88%','5 / 8','PS']].map(([name,count,rate,capacity,initials]) => <tr key={name}><td className="px-5 py-4"><div className="flex items-center gap-3"><Avatar initials={initials} /><span className="text-xs font-medium">{name}</span></div></td><td className="px-5 py-4 text-xs text-slate-600">{count}</td><td className="px-5 py-4 text-xs text-slate-600">{rate}</td><td className="px-5 py-4"><div className="flex items-center gap-3"><div className="h-2 w-32 rounded-full bg-slate-100"><div className="h-2 rounded-full bg-indigo-500" style={{ width: `${Number(capacity.split(' / ')[0]) / Number(capacity.split(' / ')[1]) * 100}%` }} /></div><span className="text-[11px] text-slate-500">{capacity}</span></div></td><td className="px-5"><MoreHorizontal size={15} className="text-slate-400" /></td></tr>)}</tbody></table></div></Page>}
        {active === 'Call Log' && <Page title="Call Log" subtitle="Review every reminder attempt and outcome."><div className="divide-y divide-slate-100 overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm">{activity.concat(activity).map(([time,title,name,detail],i) => <div key={i} className="flex items-center justify-between p-4"><div className="flex items-center gap-3"><div className="grid size-8 place-items-center rounded-lg bg-indigo-50 text-indigo-600"><Phone size={14} /></div><div><p className="text-xs font-medium text-slate-700">{title} · {name}</p><p className="mt-1 text-[11px] text-slate-400">{detail}</p></div></div><span className="text-[11px] text-slate-400">Today, {time}</span></div>)}</div></Page>}
        {active === 'Settings' && <Page title="Settings" subtitle="Tune reminders, scripts, and workspace preferences."><div className="grid max-w-4xl gap-4"><Setting icon={CalendarDays} title="Calendar integration" detail="Google Calendar · North Clinic" action="Reconnect calendar" onClick={() => notify('Calendar reconnected')} /><Setting icon={Clock3} title="Call timing" detail="Start confirmation calls before each appointment." action="24 hours before" onClick={() => notify('Call timing saved')} /><div className="rounded-lg border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-3"><div className="grid size-9 place-items-center rounded-lg bg-indigo-50 text-indigo-600"><FileText size={17} /></div><div><h2 className="text-sm font-semibold">Call script</h2><p className="mt-1 text-[11px] text-slate-500">{"Use tokens like {{client_name}}, {{service}}, and {{time}}."}</p></div></div><textarea defaultValue="Hi {{client_name}}, this is NoShowGuard calling on behalf of North Clinic about your {{service}} at {{time}}. Can we count on you to make it?" className="mt-4 min-h-28 w-full resize-none rounded-lg border border-slate-200 bg-slate-50 p-3 text-xs leading-relaxed outline-none focus:border-indigo-400" /><button onClick={() => notify('Call script saved')} className="mt-3 rounded-lg bg-indigo-600 px-3 py-2 text-[11px] font-semibold text-white">Save changes</button></div><Setting icon={Zap} title="CALL-E account" detail="1,842 calls used · 658 remaining this cycle" action="View usage" onClick={() => notify('Usage details opened')} /></div></Page>}
      </main>
    </div>
    {selected && <Detail appointment={selected} tab={detailTab} setTab={setDetailTab} close={() => setSelected(null)} notify={notify} />}
    {showAdd && <AddDialog close={() => setShowAdd(false)} notify={notify} />}
    {showCommand && <div className="fixed inset-0 z-50 grid place-items-start bg-slate-900/20 p-4 pt-24" onClick={() => setShowCommand(false)}><div onClick={e => e.stopPropagation()} className="w-full max-w-lg rounded-lg border border-slate-200 bg-white p-2 shadow-xl"><div className="flex items-center gap-2 border-b border-slate-100 px-3 py-2"><Command size={16} className="text-slate-400" /><input autoFocus placeholder="Search pages, appointments, actions..." className="flex-1 text-sm outline-none" onChange={e => setQuery(e.target.value)} /></div>{nav.map(([label,Icon]) => <button key={label} onClick={() => { setActive(label); setShowCommand(false) }} className="flex w-full items-center gap-3 rounded-md px-3 py-3 text-left text-sm hover:bg-slate-50"><Icon size={16} className="text-indigo-600" />{label}</button>)}</div></div>}
    {toast && <div className="fixed bottom-5 right-5 z-[60] rounded-lg bg-slate-900 px-4 py-3 text-xs font-medium text-white shadow-lg">{toast}</div>}
  </div>
}

function SlidersHorizontalIcon() { return <span className="text-[13px]">☷</span> }
function Page({ title, subtitle, children }: { title: string; subtitle: string; children: React.ReactNode }) { return <div><div className="mb-6"><h1 className="text-2xl font-semibold tracking-tight">{title}</h1><p className="mt-1 text-sm text-slate-500">{subtitle}</p></div>{children}</div> }
function Setting({ icon: Icon, title, detail, action, onClick }: { icon: typeof CalendarDays; title: string; detail: string; action: string; onClick: () => void }) { return <div className="flex flex-col gap-4 rounded-lg border border-slate-200 bg-white p-5 shadow-sm sm:flex-row sm:items-center"><div className="grid size-9 place-items-center rounded-lg bg-indigo-50 text-indigo-600"><Icon size={17} /></div><div className="flex-1"><h2 className="text-sm font-semibold">{title}</h2><p className="mt-1 text-[11px] text-slate-500">{detail}</p></div><button onClick={onClick} className="rounded-lg border border-slate-200 px-3 py-2 text-[11px] font-medium text-slate-600 hover:bg-slate-50">{action}</button></div> }
function Detail({ appointment: a, tab, setTab, close, notify }: { appointment: Appointment; tab: string; setTab: (v: string) => void; close: () => void; notify: (v: string) => void }) { return <div className="fixed inset-0 z-40 bg-slate-900/20" onClick={close}><section onClick={e => e.stopPropagation()} className="absolute right-0 top-0 h-full w-full max-w-[500px] overflow-y-auto border-l border-slate-200 bg-white shadow-2xl"><div className="flex items-start justify-between border-b border-slate-200 p-5"><div><p className="text-[10px] font-semibold uppercase tracking-widest text-indigo-600">Appointment details</p><div className="mt-2 flex items-center gap-3"><Avatar initials={a.initials} /><div><h2 className="text-lg font-semibold">{a.name}</h2><p className="text-[11px] text-slate-500">{a.phone} · {a.service}</p></div></div></div><button onClick={close} className="grid size-8 place-items-center rounded-lg text-slate-400 hover:bg-slate-100"><X size={17} /></button></div><div className="grid grid-cols-3 gap-px border-b border-slate-200 bg-slate-200"><div className="bg-white p-4"><p className="text-[10px] text-slate-400">When</p><p className="mt-1 text-xs font-medium">{a.date}, {a.time}</p></div><div className="bg-white p-4"><p className="text-[10px] text-slate-400">Assigned to</p><p className="mt-1 text-xs font-medium">{a.staff}</p></div><div className="bg-white p-4"><p className="text-[10px] text-slate-400">Attempts</p><p className="mt-1 text-xs font-medium">{a.attempts} of 3</p></div></div><div className="p-5"><StatusPill status={a.status} /><div className="mt-5 rounded-lg border border-indigo-100 bg-indigo-50 p-4"><div className="flex items-center gap-2 text-[11px] font-semibold text-indigo-800"><Zap size={14} /> Meeting prep</div><p className="mt-2 text-xs leading-relaxed text-indigo-950/80">{a.prep}</p></div><div className="mt-5 flex gap-1 rounded-lg bg-slate-100 p-1"><button onClick={() => setTab('Conversation')} className={`flex-1 rounded-md py-2 text-[11px] font-medium ${tab === 'Conversation' ? 'bg-white text-slate-800 shadow-sm' : 'text-slate-500'}`}>Conversation</button><button onClick={() => setTab('Details')} className={`flex-1 rounded-md py-2 text-[11px] font-medium ${tab === 'Details' ? 'bg-white text-slate-800 shadow-sm' : 'text-slate-500'}`}>Details</button></div>{tab === 'Conversation' ? <div className="mt-5 flex flex-col gap-3"><div className="max-w-[85%] rounded-lg bg-slate-100 p-3"><p className="text-[10px] font-semibold text-slate-500">NoShowGuard · 8:40 AM</p><p className="mt-1 text-xs leading-relaxed text-slate-700">Hi {a.name.split(' ')[0]}, this is a reminder for your {a.service.toLowerCase()} at {a.time}. Can we count on you to make it?</p></div><div className="ml-auto max-w-[85%] rounded-lg bg-indigo-600 p-3 text-white"><p className="text-[10px] font-medium text-indigo-200">{a.name} · 8:41 AM</p><p className="mt-1 text-xs leading-relaxed">Yes, I&apos;ll be there. Thank you for the reminder.</p></div><p className="text-center text-[10px] text-slate-400">Call completed · {a.last}</p></div> : <div className="mt-5"><h3 className="text-xs font-semibold">Attempt history</h3><div className="mt-4 flex flex-col gap-4">{['Appointment imported from Google Calendar','Reminder call placed','Client response recorded'].map((step,i) => <div key={step} className="flex gap-3"><div className="relative"><span className="grid size-6 place-items-center rounded-full bg-emerald-50 text-emerald-600"><Check size={13} /></span>{i < 2 && <span className="absolute left-3 top-6 h-5 w-px bg-slate-200" />}</div><div><p className="text-xs font-medium">{step}</p><p className="mt-0.5 text-[10px] text-slate-400">{i === 0 ? 'Yesterday, 2:04 PM' : a.last}</p></div></div>)}</div><label className="mt-6 block text-[11px] font-medium text-slate-600">Reschedule appointment<input defaultValue={`${a.date}, ${a.time}`} className="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-xs outline-none focus:border-indigo-400" /></label></div>}</div><div className="flex gap-2 border-t border-slate-200 p-5"><button disabled={a.attempts >= 3} onClick={() => notify(a.attempts >= 3 ? 'Retry limit reached' : 'Retry call queued')} className="flex-1 rounded-lg border border-slate-200 px-3 py-2.5 text-[11px] font-semibold text-slate-700 disabled:cursor-not-allowed disabled:bg-slate-50 disabled:text-slate-300">Retry call{a.attempts >= 3 && ' · max attempts'}</button><button onClick={() => { notify('Appointment marked resolved'); close() }} className="flex-1 rounded-lg bg-indigo-600 px-3 py-2.5 text-[11px] font-semibold text-white">Mark resolved manually</button></div></section></div> }
function AddDialog({ close, notify }: { close: () => void; notify: (v: string) => void }) { return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-900/20 p-4" onClick={close}><div onClick={e => e.stopPropagation()} className="w-full max-w-lg rounded-lg border border-slate-200 bg-white p-5 shadow-xl"><div className="flex items-center justify-between"><div><h2 className="text-lg font-semibold">Add appointment</h2><p className="mt-1 text-xs text-slate-500">Schedule the confirmation call with the right staff member.</p></div><button onClick={close} className="text-slate-400"><X size={18} /></button></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-[11px] font-medium text-slate-600">Client name<input className="mt-1.5 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-xs outline-none" placeholder="Jordan Miller" /></label><label className="text-[11px] font-medium text-slate-600">Phone<input className="mt-1.5 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-xs outline-none" placeholder="(415) 555-0198" /></label><label className="text-[11px] font-medium text-slate-600">Service<select className="mt-1.5 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-xs outline-none"><option>Initial consultation</option><option>Follow-up visit</option><option>Annual check-up</option></select></label><label className="text-[11px] font-medium text-slate-600">Assigned staff<select className="mt-1.5 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-xs outline-none"><option>Alex Kim</option><option>Maya Chen</option><option>Priya Shah</option></select></label><label className="text-[11px] font-medium text-slate-600">Date & time<input type="datetime-local" className="mt-1.5 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-xs outline-none" /></label></div><div className="mt-6 flex items-center justify-between"><button onClick={() => notify('CSV import opened')} className="text-[11px] font-medium text-indigo-600">or bulk import via CSV</button><button onClick={() => { notify('Appointment added and call scheduled'); close() }} className="rounded-lg bg-indigo-600 px-4 py-2.5 text-[11px] font-semibold text-white">Add & Schedule Call</button></div></div></div> }
